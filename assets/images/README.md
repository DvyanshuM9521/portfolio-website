Place project screenshots here with these filenames to match the site.

Preferred names (the site will prefer PNG/JPEG if present, otherwise fallback to the SVG placeholders):

- lexora.png  (or lexora.jpg) — will display for Lexora AI
- sentinel.png (or sentinel.jpg) — will display for SentinelAI SOC
- microgrid.png (or microgrid.jpg) — will display for Self-Healing Microgrid

Fallback SVG placeholders are already present: `lexora.svg`, `sentinel.svg`, `microgrid.svg`.

To show your real screenshots: copy your image files into this folder and name them exactly as above. The site will automatically use them on next refresh.